/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['server-api.jap.bio', 'https://server-api.jap.bio'],
  },
}

module.exports = nextConfig
